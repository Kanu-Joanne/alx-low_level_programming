#include "main.h"

/**
 * _islower - a function checks for lowercase letters
 * @c: character to be checked
 * Return: 1 if c is lowercase, else 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
