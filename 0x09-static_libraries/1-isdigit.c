#include "main.h"

/**
 * _isdigit - a function to check for a digit from 0 through 9
 * @c: integer value
 * Return: 1 if c is a digit, else 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
